# Roadmap

- [x] Convert dashboard Start timer / Log time manually forms to button-anchored dropdowns (no scrolling)
- [x] Make "Log time manually" the primary button (users mostly log manually); Start timer becomes secondary
- [x] Admin-only Revenue page: per-client hourly rate (CAD, admin-only table), earned vs invoiced totals, chart, CSV export
